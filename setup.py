from setuptools import setup

setup(
   name='RRT',
   version='1.0',
   description='Rapidly Exploring Random Tree Algorithm',
   author='J. Schmidt',
   author_email='',
   packages=['src'],
   install_requires=['sys', 'random', 'math', 'pygame', 'enum'],
)
